load("//:defs.bzl", "codex_rust_crate")

codex_rust_crate(
    name = "tui-composer",
    crate_features = ["test-support"],
    crate_name = "codex_tui_composer",
    test_data_extra = glob([
        "src/**/snapshots/**",
    ]),
    test_shard_counts = {
        "tui-composer-unit-tests": 4,
    },
)
