load("//:defs.bzl", "MACOS_WEBRTC_RUSTC_LINK_FLAGS", "xedoc_rust_crate")

xedoc_rust_crate(
    name = "tui",
    compile_data = glob(
        include = ["**"],
        allow_empty = True,
        exclude = [
            "**/* *",
            "BUILD.bazel",
            "Cargo.toml",
        ],
    ) + [
        "//xedoc-rs:Cargo.toml",
        "//xedoc-rs/collaboration-mode-templates:templates/default.md",
        "//xedoc-rs/collaboration-mode-templates:templates/plan.md",
    ],
    crate_name = "xedoc_tui",
    extra_binaries = [
        "//xedoc-rs/cli:xedoc",
    ],
    rustc_flags_extra = MACOS_WEBRTC_RUSTC_LINK_FLAGS,
    test_data_extra = glob([
        "src/**/*.rs",
        "src/**/snapshots/**",
    ]),
    test_shard_counts = {
        "tui-unit-tests": 8,
    },
)
