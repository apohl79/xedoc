load("//:defs.bzl", "xedoc_rust_crate")

xedoc_rust_crate(
    name = "tui-transcript",
    compile_data = ["tooltips.txt"] + glob([
        "src/inline_visualization/assets/**",
    ]),
    crate_features = ["test-support"],
    crate_name = "xedoc_tui_transcript",
    test_data_extra = glob([
        "src/**/*.rs",
        "src/**/snapshots/**",
    ]),
    test_shard_counts = {
        "tui-transcript-unit-tests": 4,
    },
)
