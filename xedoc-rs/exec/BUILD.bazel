load("//:defs.bzl", "xedoc_rust_crate")

xedoc_rust_crate(
    name = "exec",
    crate_name = "xedoc_exec",
    test_shard_counts = {
        "exec-all-test": 8,
    },
    test_tags = ["no-sandbox"],
)
