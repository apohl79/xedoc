{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "SessionExtensionCommand": {
      "description": "One approved plugin extension slash command.",
      "properties": {
        "description": {
          "type": "string"
        },
        "extensionId": {
          "type": "string"
        },
        "name": {
          "type": "string"
        }
      },
      "required": [
        "description",
        "extensionId",
        "name"
      ],
      "type": "object"
    }
  },
  "description": "Approved slash commands for one loaded root thread.",
  "properties": {
    "commands": {
      "items": {
        "$ref": "#/definitions/SessionExtensionCommand"
      },
      "type": "array"
    }
  },
  "required": [
    "commands"
  ],
  "title": "SessionExtensionListResponse",
  "type": "object"
}