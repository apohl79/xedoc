{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Reads the approved session-extension commands for one loaded root thread.",
  "properties": {
    "threadId": {
      "type": "string"
    }
  },
  "required": [
    "threadId"
  ],
  "title": "SessionExtensionListParams",
  "type": "object"
}